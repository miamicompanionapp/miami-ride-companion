Miami Ride Companion — Backlog & Working Notes
================================================
Last updated: 2026-06-08
Owner: Abdullah


WORKING POLICY
--------------
- PUSH STATUS: Pushing to origin/main is the deploy workflow. Every push triggers
  Workers Builds → `wrangler deploy` → live in ~30-60s. Bump the SW cache version
  on any push that changes public/ assets. Prefer small reviewable commits.
  (Cost note: free-tier build/invocation credits — don't push no-op commits.)
- This is a WORKER project, NOT Pages: deploy = `wrangler deploy` (wrangler.jsonc
  + src/index.js routes /api/* to the functions/api/* modules).
- PROD SECRETS: ANTHROPIC_API_KEY + TICKETMASTER_KEY must be encrypted SECRETS in
  the Worker's runtime "Variables and Secrets" — `wrangler deploy` on push wipes
  plain-text vars; Build-tab vars are build-time only.
- Local dev: `python -m http.server 8000` inside ./public for static testing.
  RSS refetch and Claude auto-translate require `npm run dev` (Wrangler).


NEXT UP — open items, ranked  (updated 2026-06-08)
---------------------------------------------------
Full notes for each item live in the numbered entries below (search "[ ] N.").
Re-order freely; mark [X] when done.

  1. #7   Test the full redesign on the real iPad
  2. #34  BUG: driver bio photo cropped (shoulders cut)
  3. #36  BUG: landing images blink on every GPS re-render in a moving car
  4. #30  Multiplayer games — e) Dots & Boxes, f) tug-of-war, bonus finger-picker
  5. #21  Optimize heavy venue images (mandolin.png ~1.3 MB, alacati.jpg ~807 KB)
  6. #23  Passenger feedback (storage decision needed)
  7. #17  Events auto-refresh via Cloudflare Cron (deferred)
  8. #29  Games i18n — Word Puzzle (a) still pending; content.json migration (c) future


TEST SUITE & TAGS (Playwright)
------------------------------
Run all with `npm test`. TAGGED for fast targeted regressions:

    npm run test:index     # whole passenger app (index.html)
    npm run test:games     # all 5 games (logic + i18n + flows)
    npm run test:editor    # driver dashboard (editor.html)
    npm run test:backend   # Cloudflare Functions pure logic
    npm run test:weather / test:content / test:phone / test:rest  # index sub-areas
    npm run test:smoke     # fast critical-path subset
    npm run test:negative  # all failure-path tests
    npm run test:i18n      # translation / language-switch tests
    npm run test:units     # all in-browser pure-function unit tests

Tag taxonomy:
  PAGE  : @index @editor @games @backend  (+ sub-areas @weather @content @phone @rest)
  TYPE  : @unit (pure-fn) · @negative (failure paths) · @smoke · @i18n

Layout:
  - Pure logic lives in *-units.spec.js + backend.spec.js (base @playwright/test
    runner — CDN blips can't fail a pure assertion).
  - Behavioral E2E flows use ./fixtures, which FAILS on any console/page error —
    that strict gate catches silent breakage; keep it on flow specs only.
  - backend.spec.js imports functions/api/* helpers directly (test-only `export {…}`).
    No live network.

Freshness canary: tests/passenger.spec.js "published events are all upcoming"
goes RED once published events age past today — intentional, nudges a re-fetch
+ "Remove past" in the editor.


WRANGLER SETUP (one-time, DONE 2026-05-28)
------------------------------------------
Usage:
    npm run dev         # starts Wrangler at http://localhost:8788/
    npm run dev:lan     # binds 0.0.0.0 for iPad LAN testing
No Cloudflare credits used — Wrangler executes Functions locally against your
filesystem; outbound calls (RSS feeds, Claude API) go from your machine.


OPEN ITEMS
----------

[ ] 7. Test the redesign on the actual iPad.
       - Only after all features ready (batch one push to prod).
       - For local-network preview without push:
             npm run dev:lan
         then iPad Safari → http://<laptop-LAN-IP>:8788/
       - Caveat: SW won't register over plain HTTP on a LAN IP (only localhost or
         HTTPS). Layout/UX testing works; offline/PWA install testing needs prod.
       - Key things to verify: Deco Dusk theme readability in bright car, dark-screen
         glare; tap targets; bio photo crop (#34); image blink (#36).

[ ] 17. Events auto-refresh (Cloudflare Cron) — FUTURE ENHANCEMENT.
       - Once the editor-triggered Ticketmaster flow is trusted, add a Cloudflare
         Cron Trigger that runs daily: fetch TM → map → auto-translate → commit
         content.json via GitHub API → redeploys. Set-and-forget.
       - Trade-off to weigh: daily auto-commits (a build/day, minor credits) + NO
         human quality gate (odd events could surface live). Mitigation: cron
         writes to a "pending" list the editor surfaces for one-tap approval.

[ ] 21. Optimize venue images (added 2026-05-30).
        - public/images/venues/mandolin.png ~1.3 MB, alacati.jpg ~807 KB —
          heavy for venue cards (elturco.avif/webp ones are already small).
        - Downscale + recompress with sharp (max ~1000px, q~80, mozjpeg), convert
          PNG → JPG/WebP, update content.json photo refs. Target ~100-150 KB each.

[ ] 23. Passenger feedback function.
        - A way for passengers to leave feedback (rating / quick note) from the app.
        - Decide storage: on-device IndexedDB like analytics (read on tablet) vs.
          a real endpoint (Worker → somewhere). Keep it low-friction and respect
          the no-soliciting / no-tip-prompt rules.

[~] 29. Games i18n — Word Puzzle + migration still pending.
        - DONE: Trivia, Facts Spinner, Guess the Image, Tap Frenzy.
        - PENDING:
          a) WORD PUZZLE i18n — deferred: answer is an English word the rider
             types (BISCAYNE, SUNSCREEN…); translating the clue while keeping an
             English answer is awkward. Needs a real design (per-language word lists?).
          c) Optionally migrate all game content into content.json + editor panel
             so Abdullah can edit/translate questions himself.

[~] 30. Multiplayer games — e/f/bonus still open.
        - DONE: a) Tic-tac-toe (2P), b) Tap Duel (2-3P), c) Trivia Buzzer (2-3P),
                d) Connect Four (2P).
        - OPEN:
          e) Dots and Boxes (2-3P) — natively scales to 3P (each player a color,
             most boxes wins); the one board game that scales to a third player.
          f) 3-zone button-mash tug-of-war (2-3P) — marker in center, mash to pull.
          BONUS: finger-picker / "who's it" (Chwazi pattern) — ~1hr to build, very
            on-brand for groups. Everyone puts a finger on screen, one highlighted.
        - Layout rule: players sit side-by-side SAME direction — do NOT rotate any
          player's side 180°. P1=left, P2=right (thirds for 3P).
        - SKIP Battleship-style hidden-info games — need a private screen.
        - Keep game content/logic in index.html (storage decision, see #29c).

[ ] 34. BUG: driver bio photo is cropped — shoulders cut off.
        - index.html .dp-bio-photo img uses object-fit:cover; object-position:
          center 22%. Cover fills the tall narrow box and crops the bottom; the
          22% bias keeps the face high, worsening the shoulder crop.
        - Fix: lower object-position (e.g. center 40-50%) or switch to
          object-fit:contain (whole photo, adds gradient margins). 1-line change
          but needs on-device eyeball against the real photo — don't tune blind.

[ ] 36. BUG: landing images blink/reload on GPS re-render (moving car).
        - Root cause: startGPS() uses watchPosition({enableHighAccuracy:true,
          maximumAge:30000}). In a moving car the callback fires every few seconds,
          each fire runs renderGuide() → rebuilds cards' innerHTML → destroys +
          recreates every <img> → browser re-decodes → visible blink.
        - DO NOT FIX YET per Abdullah. Candidate fixes when ready:
            * Update venue distances in place instead of rebuilding image DOM.
            * Throttle/debounce GPS-driven re-renders.
            * Skip renderGuide when sort ORDER is unchanged.
            * Skip re-render on the featured/home view (curated, not distance-sorted).
            * Revisit maximumAge / whether watchPosition needs high accuracy at all.


KNOWN TRANSITIONAL STATE (not bugs)
-----------------------------------
- Driver-page i18n labels use `tx()` helper directly (not `t()` keyed lookup which
  only reads CONTENT.strings). Follow `tx()` pattern for new driver-page strings.
- CONTENT.guide.featuredVenueId defaults to "v003" (Mandolin). If deleted, dropdown
  auto-falls back to "" (Auto = nearest Local Favorite).
- ES/PT/FR translations: all passenger-facing strings translated. Only blank-
  translation objects left are ads.sponsored[0..1] (EN also empty — unused slots).
  Enforced by tests/content.spec.js.
- Service Worker cache version: check sw.js before pushing — bump it whenever
  public/ assets change so existing tablet clients re-cache the new bundle.


DEPERSONALIZATION RULES (locked decision, do not regress)
---------------------------------------------------------
Driven by Abdullah's wife's "soliciting" critique. Public-facing UI must read
as editorial / curated, not personally branded:
  - "Local favorite" / "Local Favorites" / "Today's top pick" / "Featured" —
    NEVER "Abdullah's [anything]" in passenger-facing strings.
  - Italic personal notes on cards STAY — first-person voice without name
    attribution is curator personality, not soliciting.
  - Sidebar driver bubble subtitle is just "Your driver" (no handyman mention
    until the user taps through).
  - No tip prompts anywhere — Lyft/Uber prompts post-ride.
  - The Meet-Your-Driver page itself is fully personalized (passengers chose to
    land there).


ARCHITECTURE NOTE
-----------------
Single-file index.html (~2800+ lines including CSS + JS) and editor.html (~1500
lines). No build step. SW caches files verbatim.

Decision (2026-05-28): KEEP the monolith. Don't migrate to a framework.
If/when navigation pain forces a refactor: split into native ES modules (no bundler)
before reaching for React/Vue/Svelte/Vite. The cost of a build pipeline hugely
outweighs the benefit at this scale.
